Drop the hero image here as `hero-teacher.png` (transparent background,
PNG or WEBP). Recommended: a portrait-ish crop, roughly 900–1200px tall,
person/subject positioned toward the bottom of the frame so it anchors
against the ranking card in the hero — see app/page.tsx for how it's
positioned and sized.
